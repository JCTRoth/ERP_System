# ── Orders Service (port 5004) ────────────────────────────────────────────
apiVersion: v1
kind: Service
metadata:
  name: erp-orders-service
  namespace: erp
  labels:
    app: erp-orders-service
spec:
  type: ClusterIP
  selector:
    app: erp-orders-service
  ports:
    - port: 5004
      targetPort: 5004
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: erp-orders-service
  namespace: erp
  labels:
    app: erp-orders-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: erp-orders-service
  template:
    metadata:
      labels:
        app: erp-orders-service
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: orders-service
          image: ghcr.io/jctroth/erp-orders-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 5004
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: ASPNETCORE_URLS
              value: "http://*:5004"
            - name: ConnectionStrings__DefaultConnection
              value: "Host=erp-postgres;Port=5432;Database=ordersdb;Username=erp_orders;Password=Ra$hWee5Pei1e$"
            - name: Database__EnableSeeding
              value: "true"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z erp-postgres 5432; do echo "Waiting for postgres..."; sleep 3; done']
