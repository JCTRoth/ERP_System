apiVersion: 1

providers:
  - name: 'ERP Dashboards'
    orgId: 1
    folder: 'ERP System'
    folderUid: 'erp-system'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
